# Upstream files

Run `../fetch_reference.sh` from this directory's parent to acquire the pinned
DiffPy-CMI inputs.  The files are intentionally not committed until their
redistribution terms have been confirmed.  SHA-256 checksums and the exact
upstream commit are recorded in `../manifest.toml`.
