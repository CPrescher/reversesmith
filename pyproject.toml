[project]
name = "rsmith-python-tools"
version = "0.0.0"
description = "Optional Python tooling for rsmith ML potential backends"
requires-python = ">=3.10,<3.13"
dependencies = []

[dependency-groups]
mace = [
    "ase>=3.29",
    "mace-torch>=0.3.16",
    "torch>=2.2",
]

[tool.uv]
package = false
